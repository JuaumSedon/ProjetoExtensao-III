<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Remover Serviços</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap');

        body {
            margin: 0;
            padding: 30px;
            font-family: 'Montserrat', sans-serif;
            background-color: #4a4a4a;
            color: #ffffff;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        h1 {
            color: #ff6b6b; /* Vermelho para indicar perigo/remoção */
            text-align: center;
            font-size: 2.5em;
            margin-bottom: 30px;
        }

        .links-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-bottom: 40px;
        }

        .cta-button {
            background-color: #33d4e2;
            color: #ffffff;
            border: none;
            padding: 12px 25px;
            font-size: 0.9em;
            font-weight: bold;
            text-transform: uppercase;
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            text-decoration: none;
        }

        .cta-button:hover {
            background-color: #2abac6;
        }

        .grid-container {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 25px;
        }

        .item-card {
            background-color: #3c3c3c;
            border: 2px solid #ff6b6b; /* Borda vermelha para indicar área de remoção */
            border-radius: 8px;
            padding: 25px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
            display: flex;
            flex-direction: column;
            position: relative; /* Necessário para posicionar o botão */
        }

        .item-card h3 {
            color: #ff6b6b;
            margin-top: 10px;
            font-size: 1.5em;
        }

        .item-card .description {
            flex-grow: 1; 
            margin-bottom: 20px;
            opacity: 0.9;
            line-height: 1.6;
        }

        .item-card .price {
            font-size: 1.3em;
            font-weight: bold;
            color: #ffffff;
            text-align: right;
        }

        /* ESTILO DO BOTÃO DE REMOVER */
        .delete-form {
            width: 100%;
            margin-bottom: 10px;
        }

        .btn-remover {
            background-color: #ff4757;
            color: white;
            border: none;
            padding: 10px;
            width: 100%;
            border-radius: 5px;
            font-weight: bold;
            text-transform: uppercase;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
        }

        .btn-remover:hover {
            background-color: #c0392b;
            transform: scale(1.02);
            box-shadow: 0 0 10px rgba(255, 71, 87, 0.5);
        }

        .no-items {
            text-align: center;
            font-size: 1.2em;
            background-color: #3c3c3c;
            padding: 30px;
            border-radius: 8px;
            border: 1px dashed #777;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Área de Exclusão</h1>

        <div class="links-container">
            <a href="/" class="cta-button">Voltar para Home</a>
            <a href="/listar-itens" class="cta-button">Ver Lista Comum</a>
        </div>

        
        <% if (itens.length > 0) { %>
            <div class="grid-container">

                <% itens.forEach(item => { %>
                    <div class="item-card">

                        <form action="/remover-item/<%= item._id %>" method="POST" class="delete-form" onsubmit="return confirm('Tem certeza que deseja DELETAR <%= item.nome %>? Essa ação não tem volta!');">
                            <button type="submit" class="btn-remover">
                                🗑️ Excluir Item
                            </button>
                        </form>

                        <h3><%= item.nome %></h3>
                        
                        <p class="description">
                            <%= item.descricao || 'Sem descrição.' %>
                        </p>
                        
                        <p class="price">
                            <%= item.preco.toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' }) %>
                        </p>
                    </div>
                <% }) %>
                
            </div>
        <% } else { %>
            <p class="no-items">Nenhum item para remover.</p>
        <% } %>

    </div>

</body>
</html>