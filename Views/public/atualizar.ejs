<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Editar Item</title>
    <style>
        /* ... seu css ... */
        body { background-color: #4a4a4a; color: white; font-family: sans-serif; display: flex; justify-content: center; padding-top: 50px; }
        .form-container { background-color: #3c3c3c; padding: 40px; border: 2px solid #e1b12c; border-radius: 8px; width: 400px; }
        input, textarea { width: 100%; padding: 10px; margin-bottom: 15px; }
        button { background-color: #e1b12c; border: none; padding: 15px; width: 100%; cursor: pointer; font-weight: bold; }
    </style>
</head>
<body>

    <div class="form-container">
        <h1 style="color: #e1b12c; text-align: center;">Editar Serviço</h1>

        <form action="/atualizar-item" method="POST">
            
            <label>Nome:</label>
            <input type="text" name="nome" value="<%= item.nome %>" required>

            <label>Descrição:</label>
            <textarea name="descricao" rows="3"><%= item.descricao %></textarea>

            <label>Preço:</label>
            <input type="number" name="preco" value="<%= item.preco %>" step="0.01" required>

            <button type="submit">Salvar Alterações</button>
        </form>

        <br>
        <a href="/home" style="color: white; display: block; text-align: center;">Cancelar</a>
    </div>

</body>
</html>