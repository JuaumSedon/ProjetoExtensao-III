<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Atualizar Item: <%= itens.nome %></title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap');

        body {
            font-family: 'Montserrat', sans-serif;
            background-color: #4a4a4a;
            color: #ffffff;
            display: flex;
            justify-content: center;
            align-items: center;
            padding-top: 50px;
        }

        .form-container {
            background-color: #3c3c3c;
            padding: 40px;
            /* Cor da borda alterada para indicar edição/alerta */
            border: 2px solid #ffae42; 
            border-radius: 8px;
            width: 400px;
        }

        .form-container h1 {
            /* Cor alterada para indicar Edição */
            color: #ffae42; 
            text-align: center;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 10px;
            box-sizing: border-box;
            border-radius: 5px;
            border: 1px solid #777;
            background-color: #555;
            color: #ffffff;
        }

        /* Novo estilo para o checkbox */
        .checkbox-group {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-top: 10px;
            margin-bottom: 20px;
        }
        .checkbox-group input[type="checkbox"] {
            width: auto;
        }

        .form-button {
            /* Cor do botão alterada para Edição */
            background-color: #ffae42; 
            color: #ffffff;
            border: none;
            padding: 15px;
            font-size: 1em;
            font-weight: bold;
            cursor: pointer;
            border-radius: 5px;
            width: 100%;
            transition: background-color 0.3s ease;
        }

        .form-button:hover {
            background-color: #e69500;
        }

        .back-link {
            display: block;
            text-align: center;
            margin-top: 20px;
            color: #33d4e2;
            text-decoration: none;
        }
    </style>
</head>

<body>

    <div class="form-container">
        <h1>Atualizar Item</h1>

        <form method="POST" action="/atualizar/<%= itens._id %>?_method=PUT">

            <div class="form-group">
                <label for="nome">Nome do Item:</label>
                <input type="text" id="nome" name="nome" value="<%= itens.nome %>" required>
            </div>

            <div class="form-group">
                <label for="descricao">Descrição:</label>
                <textarea id="descricao" name="descricao" rows="3"><%= itens.descricao %></textarea>
            </div>

            <div class="form-group">
                <label for="preco">Preço:</label>
                <input type="number" id="preco" name="preco" step="0.01" min="0" value="<%= itens.preco %>" required>
            </div>

            <div class="checkbox-group">
                <label for="emEstoque">Em Estoque:</label>
                <input type="checkbox" id="emEstoque" name="emEstoque" 
                    <% if (itens.emEstoque) { %> checked <% } %>>
            </div>

            <button type="submit" class="form-button">Salvar Alterações</button>
        </form>

        <a href="/listar-itens" class="back-link">Cancelar e Voltar para Lista</a>
    </div>

</body>

</html>