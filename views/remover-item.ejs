<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Remover Serviços</title>
   <link rel="stylesheet" href="/styles/atelier.css">
</head>
<body>

    <div class="atelier-card">
        <h1 class="atelier-title" style="color: #ef4444;">Excluir Itens</h1>
        
        <div class="button-group" style="margin-bottom: 20px;">
            <a href="/listar-itens" class="btn-atelier">Ver Lista Comum</a>
            <a href="/home" class="back-link">Voltar para Home</a>
        </div>

        <% if (itens.length > 0) { %>
            
            <% itens.forEach(item => { %>
                <div class="list-item" style="border-left: 4px solid #ef4444;">
                    
                    <div class="list-item-actions">
                        <div>
                            <div class="list-item-title"><%= item.nome %></div>
                            <div class="list-item-price">
                                <%= item.preco.toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' }) %>
                            </div>
                        </div>

                        <form action="/remover-item/<%= item._id %>" method="POST" onsubmit="return confirm('Tem certeza que deseja apagar <%= item.nome %>?');">
                            <button type="submit" class="btn-icon delete" title="Excluir Permanentemente">
                                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#ef4444" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                    <polyline points="3 6 5 6 21 6"></polyline>
                                    <path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path>
                                    <line x1="10" y1="11" x2="10" y2="17"></line>
                                    <line x1="14" y1="11" x2="14" y2="17"></line>
                                </svg>
                            </button>
                        </form>
                    </div>

                    <div class="list-item-desc" style="margin-top: 8px;">
                        <%= item.descricao %>
                    </div>
                </div>
            <% }) %>

        <% } else { %>
            <div class="list-item">Não há itens para remover.</div>
        <% } %>

    </div>

</body>
</html>