<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Atelier</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap');

        /* --- VARIÁVEIS (Mesma Paleta) --- */
        :root {
            --bg-color: #f4f6f8;
            --card-color: #ffffff;
            --text-primary: #2d3436;
            --text-secondary: #636e72;
            --accent-color: #6c5ce7; /* Roxo Criativo */
            --error-color: #ff7675;  /* Vermelho suave para erros */
            --input-bg: #f9f9f9;
            --border-color: #dfe6e9;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: 'Montserrat', sans-serif;
            background-color: var(--bg-color);
            background-image: radial-gradient(#e2e2e2 1px, transparent 1px);
            background-size: 20px 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: var(--text-primary);
        }

        .login-container {
            background-color: var(--card-color);
            padding: 50px 40px;
            border-radius: 20px;
            text-align: center;
            max-width: 400px; /* Um pouco mais estreito que o menu principal */
            width: 90%;
            position: relative;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.08);
            border: 1px solid rgba(0,0,0,0.02);
        }

        /* --- LOGO --- */
        .atelier-logo {
            width: 90px;
            height: 90px;
            object-fit: cover;
            border-radius: 50%;
            margin: -20px auto 15px auto;
            display: block;
            border: 4px solid var(--card-color);
            box-shadow: 0 8px 20px rgba(108, 92, 231, 0.25);
        }

        h1 {
            margin: 0 0 10px 0;
            font-size: 1.8em;
            font-weight: 700;
            color: var(--text-primary);
        }

        p.subtitle {
            margin: 0 0 30px 0;
            color: var(--text-secondary);
            font-size: 0.9em;
        }

        /* --- FORMULÁRIO --- */
        .form-group {
            margin-bottom: 20px;
            text-align: left;
        }

        .form-group label {
            display: block;
            font-size: 0.85em;
            font-weight: 600;
            color: var(--text-secondary);
            margin-bottom: 8px;
            margin-left: 5px;
        }

        .form-input {
            width: 100%;
            padding: 14px 15px;
            background-color: var(--input-bg);
            border: 1px solid var(--border-color);
            border-radius: 12px;
            font-family: 'Montserrat', sans-serif;
            font-size: 1em;
            color: var(--text-primary);
            transition: all 0.3s ease;
        }

        .form-input:focus {
            outline: none;
            border-color: var(--accent-color);
            background-color: #fff;
            box-shadow: 0 0 0 4px rgba(108, 92, 231, 0.1);
        }

        /* --- BOTÃO --- */
        .submit-btn {
            background-color: var(--accent-color);
            color: #ffffff;
            border: none;
            padding: 16px;
            font-size: 1em;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            cursor: pointer;
            border-radius: 12px;
            width: 100%;
            transition: all 0.3s ease;
            margin-top: 10px;
            box-shadow: 0 5px 15px rgba(108, 92, 231, 0.3);
        }

        .submit-btn:hover {
            background-color: #5b4cc4;
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(108, 92, 231, 0.4);
        }

        /* --- LINKS DE APOIO --- */
        .links-area {
            margin-top: 20px;
            display: flex;
            flex-direction: column;
            gap: 10px;
            font-size: 0.9em;
        }

        .links-area a {
            color: var(--text-secondary);
            text-decoration: none;
            transition: color 0.3s;
        }

        .links-area a:hover {
            color: var(--accent-color);
        }
        
        .links-area span {
            color: #b2bec3;
            font-size: 0.8em;
        }

        /* --- ALERTA DE ERRO (EJS) --- */
        .alert-error {
            background-color: #ffeaea;
            color: var(--error-color);
            padding: 10px;
            border-radius: 8px;
            margin-bottom: 20px;
            font-size: 0.9em;
            font-weight: 600;
            border: 1px solid #ffcccc;
        }

    </style>
</head>

<body>
    <div class="login-container">
        
        <img src="/imagens/atelier.png" 
             alt="Logo Atelier" 
             class="atelier-logo">

        <h1>Bem-vindo</h1>
        <p class="subtitle">Faça login para gerenciar seu atelier</p>

        <% if (typeof mensagemErro !== 'undefined' && mensagemErro) { %>
            <div class="alert-error">
                <%= mensagemErro %>
            </div>
        <% } %>

        <form action="/login" method="POST">
            <div class="form-group">
                <label for="email">E-mail</label>
                <input type="email" id="email" name="email" class="form-input" placeholder="exemplo@email.com" required>
            </div>

            <div class="form-group">
                <label for="senha">Senha</label>
                <input type="password" id="senha" name="senha" class="form-input" placeholder="••••••••" required>
            </div>


            <button type="submit" class="submit-btn">Entrar</button>
        </form>

        <div class="links-area">
            <span>________________________</span>
            <p>Não tem uma conta? <a href="/cadastro" style="font-weight: 600;">Cadastre-se</a></p>
        </div>
    </div>
</body>

</html>