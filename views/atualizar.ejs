<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Atualizar Item</title>
    <link rel="stylesheet" href="./public/atelier.css">
</head>
<body>

    <div class="atelier-card">
        <h1 class="atelier-title">Atualizar Item</h1>
        <h2 class="atelier-subtitle"><%= item.nome %></h2>

        <form method="POST" action="/atualizar/<%= item._id %>">

            <div class="form-group">
                <label class="form-label" for="nome">Nome do Item:</label>
                <input class="form-input" type="text" id="nome" name="nome" value="<%= item.nome %>" required>
            </div>

            <div class="form-group">
                <label class="form-label" for="descricao">Descrição:</label>
                <textarea class="form-textarea" id="descricao" name="descricao" rows="3"><%= item.descricao %></textarea>
            </div>

            <div class="form-group">
                <label class="form-label" for="preco">Preço (R$):</label>
                <input class="form-input" type="number" id="preco" name="preco" step="0.01" min="0" value="<%= item.preco %>" required>
            </div>
            
            <div class="form-group" style="display: flex; align-items: center; gap: 10px;">
                <input type="checkbox" id="emEstoque" name="emEstoque" <% if (item.emEstoque) { %> checked <% } %>>
                <label class="form-label" for="emEstoque" style="margin:0;">Disponível em Estoque</label>
            </div>

            <div class="button-group">
                <button type="submit" class="btn-atelier-primary">Salvar Alterações</button>
                <a href="/listar-itens" class="back-link">Cancelar e Voltar</a>
            </div>
        </form>
    </div>

</body>
</html>