<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Serviços</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap');

        body {
            margin: 0;
            padding: 30px;
            font-family: 'Montserrat', sans-serif;
            background-color: #4a4a4a;
            color: #ffffff;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        h1 {
            color: #33d4e2;
            text-align: center;
            font-size: 2.5em;
            margin-bottom: 30px;
        }

        .links-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-bottom: 40px;
        }

        .cta-button {
            background-color: #33d4e2;
            color: #ffffff;
            border: none;
            padding: 12px 25px;
            font-size: 0.9em;
            font-weight: bold;
            text-transform: uppercase;
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            text-decoration: none;
        }

        .cta-button:hover {
            background-color: #2abac6;
        }

        .grid-container {
            display: grid;
            /* Cria colunas responsivas:
               - Tenta criar colunas de 300px.
               - Se não couber, elas encolhem (minmax).
               - auto-fill preenche o espaço.
            */
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 25px;
        }

        .item-card {
            background-color: #3c3c3c;
            border: 2px solid #33d4e2;
            border-radius: 8px;
            padding: 25px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
            display: flex;
            flex-direction: column;
        }

        .item-card h3 {
            color: #33d4e2;
            margin-top: 0;
            font-size: 1.5em;
        }

        .item-card .description {
            flex-grow: 1; /* Faz a descrição ocupar o espaço disponível */
            margin-bottom: 20px;
            opacity: 0.9;
            line-height: 1.6;
        }

        .item-card .price {
            font-size: 1.3em;
            font-weight: bold;
            color: #ffffff;
            text-align: right;
        }

        .no-items {
            text-align: center;
            font-size: 1.2em;
            background-color: #3c3c3c;
            padding: 30px;
            border-radius: 8px;
            border: 1px dashed #33d4e2;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Nossos Serviços</h1>

        <div class="links-container">
            <a href="/home" class="cta-button">Voltar para Home</a>
            <a href="/novo-item" class="cta-button">Adicionar Novo Item</a>
        </div>

        <!-- 
            Aqui começa a mágica do EJS!
            'itens' é a variável que vamos passar pelo Controller.
        -->
        
        <% if (itens.length > 0) { %>
            <div class="grid-container">
                
                <!-- Fazemos um loop em cada item da lista -->
                <% itens.forEach(item => { %>
                    <div class="item-card">
                        <h3><%= item.nome %></h3>
                        
                        <p class="description">
                            <%= item.descricao || 'Este item não possui descrição.' %>
                        </p>
                        
                        <p class="price">
                            <!-- Formatamos o preço para moeda brasileira (R$) -->
                            <%= item.preco.toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' }) %>
                        </p>
                    </div>
                <% }) %>
                
            </div>
        <% } else { %>
            <!-- Mostra esta mensagem se a lista estiver vazia -->
            <p class="no-items">Nenhum item cadastrado no momento.</p>
        <% } %>

    </div>

</body>
</html>