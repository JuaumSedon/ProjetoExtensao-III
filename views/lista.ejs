<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Serviços</title>
    <link rel="stylesheet" href="./public/atelier.css">
</head>
<body>

    <div class="atelier-card">
        <h1 class="atelier-title">Nossos Serviços</h1>

        <div class="button-group" style="margin-bottom: 30px;">
            <a href="/novo-item" class="btn-atelier-primary">Adicionar Novo</a>
            <a href="/home" class="back-link" style="margin-top:0;">Voltar ao Início</a>
        </div>

        <% if (itens.length > 0) { %>
            
            <% itens.forEach(item => { %>
                <div class="list-item">
                    <div class="list-item-actions">
                        <div class="list-item-title"><%= item.nome %></div>
                        <div class="list-item-price">
                            <%= item.preco.toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' }) %>
                        </div>
                    </div>
                    
                    <div class="list-item-desc">
                        <%= item.descricao || 'Sem descrição definida.' %>
                    </div>

                    <div style="text-align: right; margin-top: 10px;">
                        <a href="/atualizar?id=<%= item._id %>" style="text-decoration: none; font-size: 0.8em; color: var(--accent-color); font-weight: 600;">
                            ✎ Editar
                        </a>
                    </div>
                </div>
            <% }) %>

        <% } else { %>
            <div class="list-item" style="text-align: center; color: var(--text-muted);">
                Nenhum item cadastrado.
            </div>
        <% } %>

    </div>

</body>
</html>