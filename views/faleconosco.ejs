<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fale Conosco - Atelier</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap');

        :root {
            --bg-color: #f4f6f8;
            --card-color: #ffffff;
            --text-primary: #2d3436;
            --text-secondary: #636e72;
            --accent-color: #6c5ce7;
            --input-bg: #f9f9f9;
            --border-color: #dfe6e9;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: 'Montserrat', sans-serif;
            background-color: var(--bg-color);
            background-image: radial-gradient(#e2e2e2 1px, transparent 1px);
            background-size: 20px 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh; /* min-height evita cortes em telas pequenas */
            padding: 20px; /* Espaço para não colar nas bordas no celular */
            color: var(--text-primary);
        }

        .modal-container {
            background-color: var(--card-color);
            padding: 40px;
            border-radius: 20px;
            text-align: center;
            max-width: 450px;
            width: 100%;
            position: relative;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.08);
            border: 1px solid rgba(0,0,0,0.02);
            margin-top: 30px;
        }

        /* --- LOGO --- */
        .atelier-logo {
            width: 80px; /* Um pouco menor na página de contato para dar espaço ao form */
            height: 80px;
            object-fit: cover;
            border-radius: 50%;
            margin: -20px auto 10px auto;
            display: block;
            border: 4px solid var(--card-color);
            box-shadow: 0 8px 20px rgba(108, 92, 231, 0.25);
        }

        .close-btn {
            position: absolute;
            top: 20px;
            right: 25px;
            color: #b2bec3;
            font-size: 28px;
            font-weight: 300;
            cursor: pointer;
            transition: color 0.3s ease;
            line-height: 1;
        }

        .close-btn:hover {
            color: var(--accent-color);
        }

        .modal-container h1 {
            color: var(--text-primary);
            margin-top: 0;
            margin-bottom: 30px;
            font-size: 1.8em;
            font-weight: 700;
        }

        .modal-container h1::after {
            content: '';
            display: block;
            width: 40px;
            height: 3px;
            background-color: var(--accent-color);
            margin: 10px auto 0;
            border-radius: 2px;
        }

        /* --- ESTILOS DO FORMULÁRIO --- */
        .form-group {
            margin-bottom: 15px;
            text-align: left;
        }

        .form-group label {
            display: block;
            font-size: 0.85em;
            font-weight: 600;
            color: var(--text-secondary);
            margin-bottom: 8px;
            margin-left: 5px;
        }

        .form-input, .form-textarea {
            width: 100%;
            padding: 12px 15px;
            background-color: var(--input-bg);
            border: 1px solid var(--border-color);
            border-radius: 10px;
            font-family: 'Montserrat', sans-serif;
            font-size: 0.95em;
            color: var(--text-primary);
            transition: all 0.3s ease;
        }

        .form-textarea {
            resize: vertical; /* Permite redimensionar apenas a altura */
            min-height: 100px;
        }

        /* Efeito de Foco nos Inputs */
        .form-input:focus, .form-textarea:focus {
            outline: none;
            border-color: var(--accent-color);
            background-color: #fff;
            box-shadow: 0 0 0 3px rgba(108, 92, 231, 0.1);
        }

        /* Botão de Enviar (Estilo Principal) */
        .submit-btn {
            background-color: var(--accent-color);
            color: #ffffff;
            border: none;
            padding: 15px 30px;
            font-size: 0.95em;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            cursor: pointer;
            border-radius: 12px;
            width: 100%;
            transition: all 0.3s ease;
            margin-top: 10px;
            box-shadow: 0 5px 15px rgba(108, 92, 231, 0.3);
        }

        .submit-btn:hover {
            background-color: #5b4cc4;
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(108, 92, 231, 0.4);
        }

        /* Link de Voltar */
        .back-link {
            display: inline-block;
            margin-top: 20px;
            color: var(--text-secondary);
            text-decoration: none;
            font-size: 0.9em;
            font-weight: 500;
            transition: color 0.3s;
        }

        .back-link:hover {
            color: var(--text-primary);
            text-decoration: underline;
        }

        /* --- REDES SOCIAIS (Opcional) --- */
        .social-icons {
            margin-top: 25px;
            display: flex;
            justify-content: center;
            gap: 15px;
        }

        .social-btn {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background-color: #f1f2f6;
            display: flex;
            justify-content: center;
            align-items: center;
            color: var(--text-secondary);
            text-decoration: none;
            font-size: 18px;
            transition: all 0.3s ease;
        }

        .social-btn:hover {
            background-color: var(--accent-color);
            color: white;
            transform: translateY(-3px);
        }

    </style>
</head>

<body>
    <div class="modal-container">
        <span class="close-btn">&times;</span>
        
        <img src="https://images.unsplash.com/photo-1520006403909-838d6b92c22e?auto=format&fit=crop&w=200&q=80" 
             alt="Logo Atelier" 
             class="atelier-logo">

        <h1>Fale Conosco</h1>

        <form action="/enviar-mensagem" method="POST">
            <div class="form-group">
                <label for="nome">Seu Nome</label>
                <input type="text" id="nome" name="nome" class="form-input" placeholder="Ex: Maria Silva" required>
            </div>

            <div class="form-group">
                <label for="email">Seu E-mail</label>
                <input type="email" id="email" name="email" class="form-input" placeholder="maria@email.com" required>
            </div>

            <div class="form-group">
                <label for="mensagem">Mensagem</label>
                <textarea id="mensagem" name="mensagem" class="form-textarea" placeholder="Como podemos ajudar você hoje?"></textarea>
            </div>

            <button type="submit" class="submit-btn">Enviar Mensagem</button>
        </form>

        <div class="social-icons">
            <a href="#" class="social-btn" title="Instagram">
                <svg xmlns="https://www.instagram.com/" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line></svg>
            </a>
            <a href="#" class="social-btn" title="WhatsApp">
                <svg xmlns="https://web.whatsapp.com/" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"></path></svg>
            </a>
        </div>

        <a href="/" class="back-link">← Voltar ao Início</a>
    </div>
    

    <script src="/scripts/acessibilidade.js"></script>
    
</body>
</html>